return function (vpcPanel)
    local data = {};
    ---------------------------- WRITE YOUR CODE HERE ---------------------------------------
    -- ARGUMENT_ID - integer value
    -- ARGUMENT_NAME - string value
    -- 
    -- if (vpcPanel:get_argument_value(ARGUMENT_ID) == 0) then
    --     data["ARGUMENT_NAME"] = "0"
    -- else
    --     data["ARGUMENT_NAME"] = "1"
    -- end
    -- 
    --
    -- -- OR
    -- data["ARGUMENT_NAME"] = vpcPanel:get_argument_value(ARGUMENT_ID)
    -- data["ARGUMENT_NAME"] = vpcPanel:get_argument_value(ARGUMENT_ID)
    -- data["ARGUMENT_NAME"] = vpcPanel:get_argument_value(ARGUMENT_ID)
    -----------------------------------------------------------------------------------------

    return data; -- Don't forget to return data from function in order to display args inside LINK TOOL
end
